{#
/**
 * @file
 * View modes inventory - Vertical media teaser - xsmall template.
 */
#}
{% if title_suffix.contextual_links is not null %}
  {{ title_suffix.contextual_links }}
{% endif %}
<{{ outer_wrapper }}{{ attributes.addClass('vertical-media-teaser-view-mode', 'xsmall', 'anchor-all', 'clearfix') }}>
  <{{ main_wrapper }} class="main-content">
    {{ content.main }}
  </{{ main_wrapper }}>
</{{ outer_wrapper }}>
